# private.nix — machine-local secrets, never committed
# Copy to private.nix and fill in real values:
#   cp private.nix.example private.nix
{
  gitName  = "Your Name";
  gitEmail = "you@example.com";
}
