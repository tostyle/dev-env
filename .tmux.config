# Set the primary prefix to Ctrl + a
set -g prefix C-a

# Set a secondary prefix to Ctrl + b (this keeps both alive)
set -g prefix2 C-b

# Ensure you can still send the commands to inner apps
bind C-a send-prefix
bind C-b send-prefix

set -g mouse on